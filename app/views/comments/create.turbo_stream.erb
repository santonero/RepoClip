<%= turbo_stream.prepend "comments", @comment %>
<%= turbo_stream.replace "ccount", partial: "comments/ccount"  %>
<%= turbo_stream.replace "comform_frame", partial: "comments/form", locals: { comment: @comment } %>
<%= turbo_stream.append_all "body" do %>
    <template data-controller="comjump"></template>
<% end %>
